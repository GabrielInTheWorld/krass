import { RingCursorDirective } from './ring-cursor.directive';

describe('RingCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new RingCursorDirective();
    expect(directive).toBeTruthy();
  });
});
